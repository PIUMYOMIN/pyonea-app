/** Round Pyonea mark — use everywhere the brand logo appears in UI. */
export const BRAND_LOGO = require('@/assets/images/logo.png');

/** Static web path (served from public/logo.png, sourced from logo.png). */
export const BRAND_LOGO_PUBLIC_URL = '/logo.png';

/** Sampled from logo.png edge pixels. */
export const BRAND_LOGO_BACKGROUND = '#3b8950';

/** Brand wordmark font — loaded in _layout.tsx / +html.tsx. */
export const BRAND_FONT_FAMILY = 'Torus-SemiBold';
