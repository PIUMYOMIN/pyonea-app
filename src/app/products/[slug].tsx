import ProductDetailRoute, {
  ErrorBoundary,
  generateStaticParams,
  loader,
} from '@/routes/seo/product-detail-route';

export { ErrorBoundary, generateStaticParams, loader };

export default ProductDetailRoute;
